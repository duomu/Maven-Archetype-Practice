#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 批量定义别名，将指定包中的类的别名定义为类名 -->
	<!--<typeAliases>
		 <package name="${package}.${artifactId}.model"/>
	</typeAliases>-->
	<!-- 定义单个别名 -->
    <!-- <typeAliases> 
        <typeAlias alias="User" type="${package}.${artifactId}.model.User"/>
    </typeAliases>  -->
    <!-- 配置映射文件:设置了自动扫描mapper配置文件，此处可省去 -->
    <!-- <mappers>
    	<mapper resource="mybatis/sqlmap/UserMapper.xml"/>
    	<package name="${package}.ssm.mapping"/>
    </mappers> -->
</configuration>   